apiVersion: v1
kind: Pod
metadata:
  namespace: pgs-upskill
  name: nginx-second
  labels:
    project: nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.17.5
      ports:
        - containerPort: 80